\subsection{Inflammation des cellules}

\begin{enumerate}
\item Mesurer la TEER de tout les puits (voir \ref{TEER})
\item Aspirer le milieu de tout les puits et remplacer par \py{Vol_depos}~$\mu L$ de milieu ou de DMEM+INF (voir \ref{milieu-inflammation}) selon le schéma de plaque ci-dessous.

\begin{pycode}
from pydoc2tex.latex_tool import create_table

create_table(plates)
\end{pycode}

\item Dans les puits du bas on ajoute 600 $\mu L$ de milieu BMEC seulement
\item Début de l'inflammation à \py{start_time_inflammation} le \py{start_day_inflammation} ème jour
\end{enumerate}

%from BioPlate import BioPlate
%for title, values, label in plates:
%    plate = BioPlate(6,4)
%    plate.set(values, merge=True)
%    print(r"\begin{table}[H]")
%    print(r"\caption{" + title + "}")
%    print(r"\resizebox{\textwidth}{!}{")
%    print(plate.table(tablefmt="latex_raw", stralign="center"))
%    print("}")
%    print(r"\label{" + label + "}")
%    print(r"\end{table}")